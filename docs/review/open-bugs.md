# Open bugs

Running log of known defects that are still open. It starts from the
24 Sep 2026 review (`erp-review.html`, finding IDs like H-07) and adds
anything found since (IDs N-xx). When one is fixed, move it to "Recently
closed" with the commit, and update the shared to-do list.

Severity is rated for an internal network with about 15 users.

## Open

| ID | Severity | Area | Problem | Where | Suggested fix |
|---|---|---|---|---|---|
| H-07 | High | Security | The seeded `admin` / `123` account still exists, with no password policy and no forced change on first login. Sign-in is now throttled. | `server/src/seed.ts`, `routes/accounts.ts` | Change the password now; enforce at least 10 characters and a change on first login. |
| H-10 | High | Reports | The "Open Sales Orders", "Open POs" and "Open Returns" presets have no status filter, so they include shipped or closed records. | `src/lib/reports/presets.ts` | Add an exclude / multi-select status filter; give each preset its default. |
| M-10 | Medium | Vendors | The Vendors page saves on every keystroke, which causes false "changed by someone else" alerts and lost typing. | `src/pages/Vendors.tsx:36-50` | Keep edits in a draft; save on blur or with a Save button. |
| M-11 | Medium | Customers / Settings | Adding a customer note also saves any half-finished address or terms edits. Clearing the lead-time field saves 0. | `Customers.tsx:77-115`, `Settings.tsx:42-52` | Apply the note to the last saved record; validate Settings before saving on blur. |
| M-12b | Medium | Pick & pack | A line released at 0 keeps its allocation, so after the partial ships the order holds that stock until someone re-allocates. | `PickPackDetail.tsx` release | Release unpicked lines' allocation on ship, or prompt for it. |
| M-13 | Medium | Planning | The backorder forecast counts all incoming PO stock toward every order. The capacity banner calls average load "capacity". | `backorderForecast.ts`, `warehouseCapacity.ts` | Allocate incoming supply in queue order; use a configured maximum capacity. |
| M-15 | Medium | Pricing | An order line is $0.00 when the customer has no override price. PO cost defaults to the sell price. Return credit uses the catalog price, not the price charged. | `LineItemsTable.tsx:62`, `PurchaseOrderForm.tsx:49`, `ReturnForm.tsx:50` | Fall back to the catalog rate; add item cost; credit from the S.O. line. |
| L-02 | Low | Analytics | "Revenue" is bookings: it includes tax and unshipped orders, and returns aren't subtracted. Inventory value uses the sell price. | analytics summary | Rename, or compute from shipments and cost. |
| L-03 | Low | Deployment | Traffic is plain HTTP, CORS allows `*`, and the frontend defaults to `localhost:4000`. | `apiClient.ts:4`, `server/src/index.ts` | Serve from one origin behind TLS and restrict CORS. |
| L-04 | Low | Money | Money math is floating point, so printed line amounts can differ from the subtotal by cents. | `types.ts lineAmount / orderSubtotal` | Calculate in integer cents and round each line. |
| L-06 | Low | Maintainability | Permissions are defined in three places, and `GenerateBOL.tsx` is 972 lines. | `permissions.ts`, server routes, `GenerateBOL.tsx` | Share one page-to-API map with a test per preset; split the BOL form from its print view. |
| N-01 | Low | Import | The inventory import's "On Purchase Order" column does nothing (on-PO is now computed from open POs). | `importParsers.ts`, Import templates | Drop the column from the template, or warn when it's filled. |
| N-02 | Low | Errors | Server validation errors (400) show as a generic "rejected as invalid" because the zod details aren't passed through. | `src/lib/apiClient.ts` | Turn `error.fieldErrors` into a readable message. |
| N-03 | Low | Permissions | Order and RA detail pages let the person who entered a record edit it, but the server requires page edit access, so the save is refused. | `OrderDetail.tsx`, `ReturnDetail.tsx` | Either allow the writer on the server or remove the carve-out in the UI. |
| N-05 | Medium | Performance | The warehouse capacity banner on Pick & Pack downloads every order shipped in the last 30 days (about 13 MB per person at 150 orders/day). With 15 people opening Pick & Pack together, it takes 6.4 s. | `WarehouseCapacityBanner.tsx`, `warehouseCapacity.ts` | Compute throughput and dwell on the server and return the few numbers the banner shows. |
| N-06 | Low | Customers | The Customers, Routing Guide and Shipping Labels pages and the customers report still load every customer with full price sheets (about 2.3 MB). That's fine at 200 customers but grows with pricing. | `Customers.tsx`, `RoutingGuide*.tsx`, `ShippingLabelCreate.tsx` | Use the summary list and load one customer on selection. |
| N-04 | Low | Errors | Some permission messages name internal page keys ("needs edit access to validation") instead of page names. | `server/src/routes/salesOrders.ts` | Map keys to the labels in `PAGE_DEFS`. |

### Round 4 (25 Sep 2026, `erp-review-4.html`)

Found in the round-4 code review. Nothing has been fixed yet. R4-xx are bugs and vulnerabilities; PF-xx are inefficiencies. The full description and failure scenario for each is in the report.

| ID | Severity | Area | Problem | Where | Suggested fix |
|---|---|---|---|---|---|
| R4-01 | High | Security | Any order-page editor can delete or invent shipment records with an ordinary order save | `server/src/routes/salesOrders.ts:125, :446-449` | Ignore shipmentHistory on PUT. Only /ship and /undo-shipment should write shipment records. |
| R4-02 | High | Security | A save that keeps the same status skips the step permissions for allocation, release and the 'checked by' stamp | `server/src/routes/salesOrders.ts:176-213, :404-419` | Decide on the server who owns each field. Only allocation, back-orders or release editors may change allocation. Only release editors may change pendingShipment, capped at what's allocated. checkedBy/checkedAt and writtenBy should come from the signed-in account, never from the browser. Longer term, give each step its own endpoint, as ship, cancel and release already have. |
| R4-03 | High | Security | Prices, tax and customer can be changed on any order, even after it ships, and the audit log can't tell | `server/src/routes/salesOrders.ts:370-375, :465` | Require order-entry or order-detail edit to change commercial fields. Lock prices once an order is released or has shipped anything. Log old and new values for price, tax and customer changes. |
| R4-04 | High | Data | Confirm Shipped takes any quantity and can use stock reserved for other orders | `server/src/routes/salesOrders.ts:518-529; src/pages/OpenPicksDetail.tsx:225-228, :258` | Refuse any line above its packed quantity or ordered minus shipped, and refuse lines that aren't in the staged pick. Cap the input at the packed quantity; to send more, re-release. |
| R4-05 | High | Data | Closing a received return frees its quantity again, so the same shipped units can be returned and restocked twice | `server/src/routes/returns.ts:97-99` | Count every RA that was received (receivedAt set). Leave out only RAs closed without being received. |
| R4-06 | High | Data | Deleting a customer detaches its orders and returns and wipes its price sheet | `server/src/routes/customers.ts:250-257; migrations: SalesOrder/ReturnAuthorization custome` | Refuse the delete when the customer has any orders or RAs, as items already do. Add an 'inactive' flag for retiring customers, and change the constraints to RESTRICT. |
| R4-07 | Medium | Security | The Import and Labels permissions grant full sales-order edit | `server/src/routes/salesOrders.ts:176-189 (ORDER_WRITE_PAGES); src/lib/permissions.ts:171` | Remove import and labels from ORDER_WRITE_PAGES. Import stays on the create-only list. |
| R4-08 | Medium | Security | Customer price sheets and routing guides are protected only by 'Customers' edit, and price changes aren't logged row by row | `server/src/routes/customers.ts:180-246` | Require customer-pricing edit to change prices or part numbers, and routing-guide edit to change the routing guide, ideally through separate endpoints. Log each price change with item, old and new price. |
| R4-09 | Medium | Data | An RA can be marked Received without restocking, and a received RA's lines can still be edited | `server/src/routes/returns.ts:207-246; src/pages/ReturnDetail.tsx:59-60, :201` | Allow Received only through Receive Return. Lock lines once the RA isn't Issued, and remove Received from the dropdown. |
| R4-10 | Medium | Data | Saving a vendor PO trusts the browser's received quantities, receiving history and status; receiving has no upper limit | `server/src/routes/vendorPurchaseOrders.ts:37, :58, :138, :176-195, :232-247` | On save, keep the stored received quantities and history, refuse to remove lines that have receipts, and set the status on the server. Cap receive at the outstanding quantity unless an explicit over-receipt is chosen. |
| R4-11 | Medium | Data | A line that has already shipped can be changed to another item or deleted, and Undo then restocks the wrong item | `server/src/routes/salesOrders.ts:370-383, :570-582` | Refuse changes to item, deletion, or a quantity below shipped on any line with shipments. Save the item on each shipment line and use that record for undo and returns. |
| R4-12 | Medium | Data | Undo Last Shipment ignores returns, so goods already returned are restocked again | `server/src/routes/salesOrders.ts:553-594` | Refuse undo (or limit it per item) when received RAs exist against the order. Also refuse undo on a Cancelled order (see R4-30). |
| R4-13 | Medium | Data | Item numbers that differ only by case can both exist, and stock lands on either one | `server/src/routes/items.ts:98-102, :144; server/src/lib/inventory.ts:10-14, :72-80` | Add a unique index on the lower-cased, trimmed item number, compare ignoring case on create and rename, and trim on save. |
| R4-14 | Medium | Screens | A blank line gets saved on the order, and that order later can't ship | `src/pages/OrderEntry.tsx:119-136; src/components/LineItemsTable.tsx:46-48; src/pages/Alloc` | Drop blank rows before saving. The server should refuse an empty item. Allocation should stop treating unknown items as unlimited. |
| R4-15 | Medium | Screens | Pressing Enter anywhere on Order Entry saves the order, and no customer is required | `src/pages/OrderEntry.tsx:178, :294-298; src/components/SearchSelect.tsx:46-56` | Enter should pick the highlighted option in the customer search and move to the next cell in the line grid. Require a customer and at least one complete line before saving. |
| R4-16 | Medium | Screens | A double-click creates duplicate vendor POs and duplicate RAs | `src/pages/PurchaseOrderForm.tsx:64-79; src/pages/ReturnForm.tsx:66-81` | Add the same saving guard Order Entry uses. Longer term, send a request key the server can use to reject repeats. |
| R4-17 | Medium | Screens | A BOL for orders going to different addresses prints no delivery address at all | `src/pages/GenerateBOL.tsx:52-61, :708, :812-823` | Print each order's ship-to in the order rows, and compare addresses ignoring case and spacing. |
| R4-18 | Medium | Screens | Order Detail lets you edit lines the server will refuse, a refusal throws away the whole draft, and back-ordered quantities can't be revised at all | `src/pages/OrderDetail.tsx:89, :107-123, :180-184; server/src/routes/salesOrders.ts:204-210` | Make lines read-only after Checked and keep the draft when a save is refused. Add a 'revise back-ordered quantity' action for analysts. |
| R4-19 | Medium | Screens | Review Queues take the newest order first, the Allocation queue ignores the chosen sort, and there's no Skip | `src/pages/Validation.tsx:16-22; src/pages/Allocation.tsx:47-51; server/src/routes/salesOrd` | Build the queue oldest-first (by due date, then order date) from the list as shown, and add Skip. Replace it with a claim-based 'next order' (P-B2) later. |
| R4-20 | Medium | Screens | Shipment History leaves out partial shipments, and there's no end-of-day shipped report | `src/pages/ShipmentHistory.tsx:35-42; src/pages/Dashboard.tsx:408-416` | Filter by ship date (default today), show one row per shipment with totals and export, and offer Undo on each row. |
| R4-21 | Medium | Screens | Schedule Shipments saves every time the date changes, including while you type the year | `src/pages/ScheduleShipments.tsx:59-74, :187-192` | Save on blur or Enter, only for a complete, plausible date. |
| R4-22 | Medium | Screens | A Checked order can be edited without being checked again | `server/src/routes/salesOrders.ts:370; src/pages/OrderDetail.tsx:107-110` | Send the order back to Entered when a line or price changes after Checked, or re-run the validation rules (P-A1). Use the same price lookup as Order Entry. |
| R4-23 | Medium | Screens | Cancelling a released order makes it vanish from Open Picks while the printed pick is still being worked | `server/src/routes/salesOrders.ts:620-631; src/pages/OpenPicks.tsx:9-16` | Keep a 'pull from floor' list in Open Picks until logistics acknowledges it, and warn Customer Service when cancelling an order that has been printed. |
| R4-24 | Low | Security | An unused counter endpoint lets any signed-in user use up or seed document numbers | `server/src/routes/counters.ts:45-58, :64-88` | Delete the route. Keep overrides between the highest issued number and a safe maximum, and log them in the audit. |
| R4-25 | Low | Security | Settings and document-number changes aren't audited, and settings accept any key and any value | `server/src/routes/settings.ts:121-139; server/src/routes/counters.ts:64-88` | Allow only known setting keys, check each value, and log every change with its old and new value. |
| R4-26 | Low | Data | Opening stock on a new or imported item isn't in the stock ledger | `server/src/routes/items.ts:103-120` | Write an 'opening' movement when an item is created with stock. |
| R4-27 | Low | Data | The cached On PO figure can drift (it matches item numbers by exact spelling, without a lock) | `server/src/lib/inventory.ts:95-111; server/src/routes/vendorPurchaseOrders.ts:133-137` | Save the catalog's spelling of the item number, total by item id, and lock the item row while totalling. |
| R4-28 | Low | Data | A database error part-way through a bulk release returns a bare error, though earlier orders have already been released | `server/src/routes/salesOrders.ts:709` | Catch every error per order, add it to the results, and keep going. |
| R4-29 | Low | Data | Analytics drops the shipped value of orders cancelled after a partial shipment (more detail on L-02) | `server/src/routes/analytics.ts:61-62, :181-188` | Base revenue and quantities on what shipped. |
| R4-30 | Low | Data | Undo Shipment works on a cancelled order and puts it back on the floor | `server/src/routes/salesOrders.ts:560-587` | Refuse undo on Cancelled orders. |
| R4-31 | Low | Screens | Confirming several picks stops at the first error, doesn't refresh, and can be clicked twice | `src/pages/OpenPicks.tsx:50-67, :150-157` | Collect a result for each order (as Release Orders does), lock the button while it runs, and always reload. |
| R4-32 | Low | Screens | Reports stay on 'Running…' forever when the server refuses the data source | `src/pages/ReportRunner.tsx:63-70` | Reset the loading state and show the error. Hide sources and actions the role can't use. |
| R4-33 | Low | Screens | Order import shows unknown items as 'Ready', then hides the reason they fail | `src/lib/importParsers.ts:335-347; src/pages/Import.tsx:122-126` | Flag unknown items in the preview, pass the server's message through, and hide import types the role can't use. |
| R4-34 | Low | Screens | Some links lead to Access Denied, and 'Returns to receive' goes to the wrong role | `src/pages/Dashboard.tsx:151, :451-467; src/components/Layout.tsx:56-67; src/pages/OrdersLi` | Show links only when the role has access. Base the returns queue on Receiving edit. |
| R4-35 | Low | Screens | Quantity fields accept decimals and negatives, and the due date can be cleared, which the server then refuses with a generic error | `src/pages/AllocationDecision.tsx:96-99; src/pages/PurchaseOrderDetail.tsx:49-51; src/compo` | Round to whole numbers, require quantity ≥ 1 and a due date, and check dates on the server. |
| R4-36 | Low | Screens | Pressing Enter in the Dashboard's Find an Order can open the previous search's result | `src/pages/Dashboard.tsx:217-228` | Only open a result once it belongs to the text currently typed. |
| R4-37 | Low | Screens | Analytics 'this month' and the capacity chart group by UTC date | `src/pages/Analytics.tsx:97-100; src/lib/warehouseCapacity.ts:39-41, :80-92` | Group by local date. |
| R4-38 | Low | Screens | Item Profile shows no preferred vendor to roles that can't view the vendor list | `src/pages/ItemProfile.tsx:45-47, :279` | Return the vendor name with the item. |
| R4-39 | Low | Security | Small hardening items | `various` | Fix each in passing. None matters much on a 15-person internal network. |
| PF-01 | High | Performance | A report with an item filter that matches nothing downloads the entire order history | `src/lib/reports/dataSources.ts:97-101, :54-78; src/pages/ReportRunner.tsx:63-75` | Return 'no rows' when the item matches nothing. Don't auto-run, and require a date range for order sources. Longer term, have the server return report rows directly. |
| PF-02 | Medium | Performance | Inventory recalculates each item against every open-order line on every keystroke | `src/pages/Inventory.tsx:83-86; src/lib/backorderForecast.ts; src/pages/BackOrderQueue.tsx:` | Build a per-item total once per data load, or add an /api/items/stock-status summary. |
| PF-03 | Medium | Performance | Receiving, the Back Order Queue and the PO reports download every vendor PO ever written | `src/pages/Receiving.tsx:13; src/pages/BackOrderQueue.tsx:25; server/src/routes/vendorPurch` | Switch Receiving and Back Orders to the open-only list, and add paging and search to the PO and RA lists. |
| PF-04 | Medium | Performance | The most-used query ('open orders') and the allocation check scan the whole order history | `server/src/routes/salesOrders.ts:235-244; server/src/lib/inventory.ts:160-163` | Filter with status IN (open statuses) or add a partial index. Index shipment date, and sort recent orders by S.O. #. |
| PF-05 | Medium | Performance | Order search by item, by free text, or sorted by ship date scans full history on every page | `server/src/routes/salesOrderSearch.ts:129-155, :186-201` | Filter by item id, keep a 'last shipped' column, and add a trigram index for customer name and P.O. |
| PF-06 | Medium | Performance | The stock ledger's item filter searches on every keystroke, and each search scans the whole ledger | `src/pages/AuditLog.tsx:184, :202; server/src/routes/stockMovements.ts:18-28` | Wait until typing pauses (the helper already exists), and filter by item id, which is indexed. |
| PF-07 | Medium | Performance | The capacity lookback has no upper limit, so one Settings typo can make every Release Orders view download a year of orders | `src/pages/Settings.tsx:48-52; src/lib/settingsStore.ts:22-23; server/src/routes/salesOrder` | Cap the lookback at 90 days, save on blur, and cap the server query. Remove the unused all-orders call. The real fix is still N-05: compute capacity on the server. |
| PF-08 | Medium | Performance | Item Quick Report and Product Labels download thousands of orders to build small lists | `src/lib/reports/dataSources.ts:465-477; src/pages/ProductLabels.tsx:55-60` | Add small server endpoints for item order lines and a customer's purchased items. |
| PF-09 | Low | Performance | Workflow pages download every open order to show one queue, and Release Orders loads the catalog and open orders twice | `src/pages/Validation.tsx:12-16; src/pages/PickPack.tsx:120-130 + WarehouseCapacityBanner.t` | Add a status filter and a slim list format, a counts endpoint for the Dashboard, shared page data, and small save endpoints for single fields (BOL, ship date). |
| PF-10 | Low | Performance | A slow old response can overwrite a newer one on Reports and Warehouse Capacity | `src/pages/ReportRunner.tsx:63-75; src/pages/WarehouseCapacity.tsx:36-38` | Tag each request and ignore stale answers, as the paged lists already do. |
| PF-11 | Low | Performance | Stock updates run one query at a time inside the transaction, and the allocation check always uses the slow case-insensitive lookup | `server/src/lib/inventory.ts:43-62, :106-111, :170-171` | Look items up in one query, apply stock changes as one statement, and key reservations by item id. |
| PF-12 | Low | Performance | Duplicate and unusable indexes; missing ones on columns that are filtered or sorted | `server/prisma/schema.prisma` | Drop the two useless indexes and add the missing ones. |
| PF-13 | Low | Performance | Responses aren't compressed, and the frontend loads as one bundle | `server/src/index.ts:27-33; src/App.tsx` | Add compression, lazy-load the rarely used pages, and serve the built files. |
| PF-14 | Low | Performance | The analytics summary reads all order lines three times per recalculation, and its cache never forgets entries | `server/src/routes/analytics.ts:276-346` | Compute order totals once, keep a monthly roll-up, serve the cached value while refreshing, and limit the cache size. |

## Recently closed

| ID | Closed by | Notes |
|---|---|---|
| C-01, C-02, C-03, H-01, H-02, H-04, H-05, H-06, H-08, M-01 … M-09, L-01 | first review round (`b5a9c4e`, `cbab33d`) | See `erp-review.html`. |
| H-03 | paging merge (`bd6caa5`) + analytics cache | Order history pages are paged; analytics is computed on the server and shared for 60 s. See N-05 and N-06 for the pieces still left. |
| H-09 | import merge (`7dd143c`) | Row-by-row save with results, duplicate detection, server-rule validation, button locked while saving. |
| M-12 | `6ba6f5f` | Returns restock on receipt, and orders can be cancelled (M-12b above is the remaining piece). |
| M-14 | `6ba6f5f` | Lines carry `itemId`; renames follow history; unknown items are rejected on save. |
